#!/bin/bash
# The compile test failed because of missing tycho classpath. The maven build failed earlier because we are missing JavaSE-25 target. We verified diff syntax and benchmark synthetically earlier.
